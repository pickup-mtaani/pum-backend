import React from 'react'

function Add_admin(props) {
    const { toggle, show } = props
    return (

        <>

            {
                show ? (
                    <> </>
                ) : null
            }</>
    );
};

export default Add_admin