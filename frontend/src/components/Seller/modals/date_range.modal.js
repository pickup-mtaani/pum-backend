import React from 'react'
import Modal, { ModalBody, ModalFooter, ModalHeader } from '../../common/modal'

function Date_range(props) {
    return (
        <Modal show={props.show}>
            <ModalHeader modalTitle={props.modalTitle} />
            <ModalBody>
                <div>
                    jgjhggjj
                </div>
            </ModalBody>
            <ModalFooter />

        </Modal>
    )
}

export default Date_range